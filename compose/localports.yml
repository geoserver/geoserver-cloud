# Used only when needing to expose local ports on geoserver backend services
# for example, when running the gateway through the IDE 
# Port numbers match the ones in the `local` spring profile used for development.
services:
  config:
    ports:
      - 8888:8080 # not needed in production, exposed for local development
  consul:
    ports:
      - 8500:8500 # for development, so services can be run from localhost and find the discovery service running on docker
  acl:
    ports:
      - 9000:8080
  gateway:
    ports:
      - 9090:8080
      - 8190:8081
  wfs:
    ports:
      - 9101:8080
      - 8101:8081
  wms:
    ports:
      - 9102:8080
      - 8102:8081
  wcs:
    ports:
      - 9103:8080
      - 8103:8081
  restconfig:
    ports:
      - 9105:8080
      - 8105:8081
  webui:
    ports:
      - 9106:8080
      - 8106:8081
  gwc:
    ports:
      - 9107:8080
      - 8107:8081
