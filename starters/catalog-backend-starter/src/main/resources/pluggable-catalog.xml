<?xml version="1.0" encoding="UTF-8"?>
<!-- 
 Copyright (C) 2020 - Open Source Geospatial Foundation. All rights reserved.
 This code is licensed under the GPL 2.0 license, available at the root
 application directory.
 -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Default CatalogFacade using the pluggable backend architecture -->
    <bean id="catalogFacade" class="org.geoserver.catalog.plugin.DefaultCatalogFacade"/>

    <!-- 
      rawCatalog, use primary=true to override the one defined in gs-main's applicationContext.xml, may it be loaded,
      and set it the default catalog facade defined above instead of the one it creates at its constructor
     -->
    <bean primary="true" id="rawCatalog" name="rawCatalog" class="org.geoserver.catalog.impl.CatalogImpl">
         <property name="facade" ref="catalogFacade"/>  
         <property name="resourceLoader" ref="resourceLoader"/>  
    </bean>
    
    <!-- 
      Bean factory that'll look up for a bean of type org.geoserver.platform.resource.ResourceStore named "resourceStoreImpl"
      in the application context and default to "dataDirectoryResourceStore" if none is found 
    -->
    <bean primary="true" id="resourceStore" class="org.geoserver.platform.resource.ResourceStoreFactory" />

    <bean primary="true" id="resourceLoader" class="org.geoserver.platform.GeoServerResourceLoader">
	    <constructor-arg ref="resourceStore"/>
    </bean>
</beans>
